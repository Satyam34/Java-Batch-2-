/* Encapsulation is defined as the wrapping up of data & methods under a single unit. It also implements data hiding. 
real life example  A Vehicle keeps its speed and fuel information hidden and provides methods to start and stop.

Polymorphism:Using the same action in different ways for different objects.
Different vehicles Car, Truck may have different horn sounds, but they all use the honk() method


Abstraction: Hiding complex details and showing only the essential features
The driver allows use to operate the vehicle without needing to understand its internal workings.

Inheritance: Creating new objects based on existing ones.
Inheritance** lets one class (like car use features from another class like Vehicle and add its own.
    */
